apiVersion: imp.dev/v1alpha1
kind: ImpVMTemplate
metadata:
  name: ci-explicit-template
  namespace: default
spec:
  classRef:
    name: ci-explicit-small
  image: ghcr.io/syscode-labs/test:latest
  networkRef:
    name: ci-explicit-net
