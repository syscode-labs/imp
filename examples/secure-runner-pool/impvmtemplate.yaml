apiVersion: imp.dev/v1alpha1
kind: ImpVMTemplate
metadata:
  name: ci-runner-template
  namespace: default
spec:
  classRef:
    name: ci-small
  networkRef:
    name: ci-net
  image: ghcr.io/syscode-labs/test:latest
  guestAgent:
    enabled: true
