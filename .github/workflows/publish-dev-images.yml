name: Publish Development Images

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: Full 40-character commit SHA reachable from main
        required: true
        type: string

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-dev-images-${{ inputs.commit_sha }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
      - id: target
        env:
          TARGET_SHA: ${{ inputs.commit_sha }}
        run: |
          set -euo pipefail
          [[ "$TARGET_SHA" =~ ^[0-9a-f]{40}$ ]]
          git fetch --no-tags origin main
          git cat-file -e "$TARGET_SHA^{commit}"
          git merge-base --is-ancestor "$TARGET_SHA" origin/main
          git checkout --detach "$TARGET_SHA"
          echo "sha=$TARGET_SHA" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: operator
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/syscode-labs/imp-operator:sha-${{ steps.target.outputs.sha }}
      - id: agent
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.agent
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/syscode-labs/imp-agent:sha-${{ steps.target.outputs.sha }}
      - env:
          SHA: ${{ steps.target.outputs.sha }}
          OPERATOR_DIGEST: ${{ steps.operator.outputs.digest }}
          AGENT_DIGEST: ${{ steps.agent.outputs.digest }}
        run: |
          jq -n --arg commit_sha "$SHA" --arg operator "ghcr.io/syscode-labs/imp-operator@$OPERATOR_DIGEST" --arg agent "ghcr.io/syscode-labs/imp-agent@$AGENT_DIGEST" '{commit_sha:$commit_sha,operator:$operator,agent:$agent}' > image-digests.json
          cat image-digests.json
      - uses: actions/upload-artifact@v4
        with:
          name: image-digests-${{ steps.target.outputs.sha }}
          path: image-digests.json
          retention-days: 90
