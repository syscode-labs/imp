name: Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag to publish (e.g. v0.2.0)"
        required: false
        type: string

env:
  GO_VERSION: "1.26"
  REGISTRY: ghcr.io
  IMAGE_BASE: ghcr.io/syscode-labs

jobs:
  release-arch:
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate CRD manifests
        run: make manifests

      - name: Build context note
        run: |
          echo "::notice title=Build context::Building linux/${{ matrix.arch }} on native runner ${{ matrix.runner }}."

      - name: Build and push operator image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.operator
          platforms: linux/${{ matrix.arch }}
          push: true
          tags: |
            ${{ env.IMAGE_BASE }}/imp-operator:${{ github.event.release.tag_name || inputs.release_tag }}-${{ matrix.arch }}
            ${{ env.IMAGE_BASE }}/imp-operator:latest-${{ matrix.arch }}

      - name: Build and push node agent image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.agent
          platforms: linux/${{ matrix.arch }}
          push: true
          tags: |
            ${{ env.IMAGE_BASE }}/imp-agent:${{ github.event.release.tag_name || inputs.release_tag }}-${{ matrix.arch }}
            ${{ env.IMAGE_BASE }}/imp-agent:latest-${{ matrix.arch }}

      - name: Build and push sandbox controller image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.sandbox
          platforms: linux/${{ matrix.arch }}
          push: true
          tags: |
            ${{ env.IMAGE_BASE }}/imp-sandbox:${{ github.event.release.tag_name || inputs.release_tag }}-${{ matrix.arch }}
            ${{ env.IMAGE_BASE }}/imp-sandbox:latest-${{ matrix.arch }}

      - name: Build and push sandbox gateway image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.gateway
          platforms: linux/${{ matrix.arch }}
          push: true
          tags: |
            ${{ env.IMAGE_BASE }}/imp-sandbox-gateway:${{ github.event.release.tag_name || inputs.release_tag }}-${{ matrix.arch }}
            ${{ env.IMAGE_BASE }}/imp-sandbox-gateway:latest-${{ matrix.arch }}

  merge-manifests:
    runs-on: ubuntu-24.04
    needs: [release-arch]
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create multi-arch manifests
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.event.release.tag_name || inputs.release_tag }}"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-operator:${TAG}" \
            "${{ env.IMAGE_BASE }}/imp-operator:${TAG}-amd64" \
            "${{ env.IMAGE_BASE }}/imp-operator:${TAG}-arm64"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-operator:latest" \
            "${{ env.IMAGE_BASE }}/imp-operator:latest-amd64" \
            "${{ env.IMAGE_BASE }}/imp-operator:latest-arm64"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-agent:${TAG}" \
            "${{ env.IMAGE_BASE }}/imp-agent:${TAG}-amd64" \
            "${{ env.IMAGE_BASE }}/imp-agent:${TAG}-arm64"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-agent:latest" \
            "${{ env.IMAGE_BASE }}/imp-agent:latest-amd64" \
            "${{ env.IMAGE_BASE }}/imp-agent:latest-arm64"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-sandbox:${TAG}" \
            "${{ env.IMAGE_BASE }}/imp-sandbox:${TAG}-amd64" \
            "${{ env.IMAGE_BASE }}/imp-sandbox:${TAG}-arm64"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-sandbox:latest" \
            "${{ env.IMAGE_BASE }}/imp-sandbox:latest-amd64" \
            "${{ env.IMAGE_BASE }}/imp-sandbox:latest-arm64"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-sandbox-gateway:${TAG}" \
            "${{ env.IMAGE_BASE }}/imp-sandbox-gateway:${TAG}-amd64" \
            "${{ env.IMAGE_BASE }}/imp-sandbox-gateway:${TAG}-arm64"

          docker buildx imagetools create \
            -t "${{ env.IMAGE_BASE }}/imp-sandbox-gateway:latest" \
            "${{ env.IMAGE_BASE }}/imp-sandbox-gateway:latest-amd64" \
            "${{ env.IMAGE_BASE }}/imp-sandbox-gateway:latest-arm64"

      - uses: azure/setup-helm@v5

      - name: Package and push sandbox Helm chart
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.event.release.tag_name || inputs.release_tag }}"
          VERSION="${TAG#v}"
          helm package charts/imp-sandbox --version "${VERSION}" --app-version "${VERSION}"
          helm push "imp-sandbox-${VERSION}.tgz" oci://${{ env.IMAGE_BASE }}/charts
